import React from 'react';
import PersonalInformation from './PersonalInformation';

const Form = () => {
    return (
        <form>
            <PersonalInformation />
        </form>
    );
};

export default Form;